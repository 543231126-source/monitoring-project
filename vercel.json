{
  "version": 2,
  "buildCommand": "composer install && php artisan config:cache && npm install && npm run build",
  "installCommand": "composer install && npm install",
  "outputDirectory": "public",
  "framework": "laravel",
  "routes": [
    { "src": "/(.*)", "dest": "/index.php" }
  ]
}
